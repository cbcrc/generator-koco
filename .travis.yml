language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-koco' ]; then cd .. && eval "mv $currentfolder generator-koco" && cd generator-koco; fi
