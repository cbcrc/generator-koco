language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-rc-framework-js' ]; then cd .. && eval "mv $currentfolder generator-rc-framework-js" && cd generator-rc-framework-js; fi
